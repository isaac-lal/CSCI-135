#include <iostream>
using namespace std;

int main() {
    int line = 10;
    string arr[10] = {"hi", "I", "k", "no", "u", "me", "them", "cs", "luv", "gf"};

    for(int i = line; i >= 0; i--) {
        cout << arr[i] << endl;
    }
}